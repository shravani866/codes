#include<stdio.h>
int main() {
int N;
scanf("%d",&N);
int arr;
for(int i=0;i<N;i++) {
scanf("%d",&arr[i]);
}
int i=0, j=N-1;
while(i<=j) {
if(i==j) {
printf("%d",arr[i]);
} else {
printf("%d %d ",arr[i],arr[j]);
}
i++;
j--;
}
printf("\n");
return 0;
}
