#include<stdio.h>
int main() {
int N,original, reversed=0, digits;
scanf("%d", &N);
original=N;
while(N>0) {
digits=N%10;
reversed=reversed*10+digits;
N/=10;
}
if(reversed==original)
printf("Palindrome\n");
else
printf("Not Palindrome\n");
return 0;
}
