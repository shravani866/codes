#include<stdio.h>
int main()
{
    int N;
    scanf("%d",&N);
    int square N*N;
    int sum=0;
    while(square>0) {
        sum+=square%10;
        square/=10;
    }
    if(sum==N) {
        printf("Neon Number
    }else {
        printf("Not Neon Number
");
    }
    return 0;
}
