#include<stdio.h>
int isPalindrome(int n) {
int original=n,reverse=0,digit;
while(n>0) {
digit=n%10;
reverse=reverse*10+digit;
n=n/10;
}
if(original==reverse)
return 1;
else
return 0;

}
int main() {
int N;
scanf("%d",&N);
if(isPalindrome (N))
printf("Palindrome
");
else
printf("Not Palindrome
");

return 0;
}
